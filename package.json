{
  "name": "whale-trucker-command-center",
  "version": "1.0.0",
  "description": "WhaleTrucker — Élite Command Center",
  "main": "index.js",
  "scripts": {
    "test": "jest --coverage",
    "lint": "eslint ."
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "eslint": "^8.0.0"
  },
  "jest": {
    "coverageReporters": ["lcov", "text"]
  }
}
